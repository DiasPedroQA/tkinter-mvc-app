# """
# Classe que representa a estrutura de dados da aplicação.
# """


# class DataModel:
#     """Classe que representa a estrutura de dados da aplicação."""

#     def __init__(self):
#         self.data = {}

#     def add_data(self, key, value):
#         """Adiciona um novo par chave-valor ao modelo de dados."""
#         self.data[key] = value

#     def get_data(self, key):
#         """Retorna o valor associado à chave fornecida."""
#         return self.data.get(key, None)

#     def remove_data(self, key):
#         """Remove o par chave-valor associado à chave fornecida."""
#         if key in self.data:
#             del self.data[key]

#     def clear_data(self):
#         """Limpa todos os dados armazenados no modelo."""
#         self.data.clear()
